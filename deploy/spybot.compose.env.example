# Copy to deploy/spybot.compose.env (gitignored) and adjust.
# Find the vahan360 compose network Caddy/web/api use:
#   docker network ls
# Typical name: <folder>_<network_key> e.g. vahan360_vahan360-network

V360_EDGE_NETWORK=vahan360_vahan360-network
